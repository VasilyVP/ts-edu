/* import { Like, FindOptionsWhereProperty, RemoveOptions, Not, MoreThan, Any, IsNull, Raw, LessThan } from 'typeorm';
import { dataSource } from './TypeORM/dataSource';
import { Album } from './TypeORM/entity/Album';
import { Photo } from './TypeORM/entity/Photo';
import { PhotoMetadata } from './TypeORM/entity/PhotoMetadata';
import { Author } from './TypeORM/entity/Author';
import { User } from './TypeORM/entity/User';
import { PhotoAndMetadata } from './TypeORM/entity/PhotoAndMetadata';
import { format } from 'path';
import axios, { AxiosError } from 'axios';

console.log('axios: ', axios) */

google.accounts.id

try {
    //const res = await axios.post(adr5, {}, { withCredentials: true });

    //console.log('res: ', res);

    await new Promise(() => { throw Error('bla bla') });

} catch (err) {
    console.error(err);
}
